{
  "meta": {
    "framework": "instructor",
    "useCase": "coding-assistant",
    "generatedAt": "2026-03-12"
  },
  "seo": {
    "title": "Building AI Coding Assistant with Instructor + Vex Guardrails",
    "description": "Learn how to build ai coding assistant with Instructor + Vex guardrails. Detect failures, prevent drift, and ensure reliability.",
    "keywords": [
      "Instructor ai coding assistant",
      "Instructor guardrails",
      "ai coding assistant reliability",
      "Vex Instructor",
      "AI agent guardrails"
    ]
  },
  "content": {
    "intro": "Building an AI Coding Assistant with Instructor enables structured extraction of code suggestions, fixes, and explanations from LLMs, ensuring reliable Pydantic-validated outputs. Guardrails are essential to verify code correctness, prevent security vulnerabilities, and block credential leakage in repository-aware agents. Vex integration catches common Instructor failures like schema validation retries and hallucinated enum values while enforcing coding safety standards.",
    "architectureOverview": {
      "heading": "AI Coding Assistant Architecture with Instructor",
      "description": "The AI Coding Assistant follows a code-aware agent loop: repository context is fed into an Instructor-patched LLM client to extract structured CodeSuggestion objects containing fixes, tests, and explanations. The sandbox executes proposed code with test validation, triggering iterative debugging cycles when validation fails. Vex verifies both Instructor outputs and sandbox results against code correctness, security, and compliance policies.\n\nInstructor wraps the LLM client to enforce Pydantic schemas for consistent extraction of code blocks, file paths, and test cases. The agent maintains conversation state across iterations, with Vex providing observability into validation failures and security risks. This creates a reliable feedback loop for production-grade code generation.",
      "diagram": "┌─────────────────┐    ┌──────────────────┐\n│   Repository    │───▶│ Instructor LLM   │\n│   Context       │    │ + Vex Verify     │\n└─────────────────┘    │  CodeSuggestion  │\n                       └──────────────────┘\n                               │\n                       ┌──────────────────┐\n                       │   Sandbox Exec   │◀──┐\n                       │ + Test Validate  │   │\n                       └──────────────────┘   │\n                               │              │\n                       ┌──────────────────┐   │\n                       │  Vex Guardrails  │───┘\n                       │ Security/Compliance│\n                       └──────────────────┘"
    },
    "risks": [
      {
        "name": "Malicious code injection",
        "description": "Instructor-extracted code contains shell commands, eval(), or filesystem writes exploiting sandbox escapes",
        "severity": "critical",
        "mitigation": "Vex static analysis + sandbox permission verification"
      },
      {
        "name": "Credential leakage in comments",
        "description": "LLM hallucinates API keys or tokens in code comments/examples during fix generation",
        "severity": "high",
        "mitigation": "Vex secret scanning on Instructor CodeSuggestion objects"
      },
      {
        "name": "Insecure dependency recommendation",
        "description": "Assistant suggests compromised or malicious PyPI/npm packages",
        "severity": "high",
        "mitigation": "Vex dependency safety database lookup"
      },
      {
        "name": "License-incompatible code",
        "description": "Extracted code mixes GPL with proprietary code violating compliance",
        "severity": "medium",
        "mitigation": "Vex license header scanning and compatibility checks"
      }
    ],
    "implementation": [
      {
        "title": "Instructor Pydantic Model for Code Suggestions",
        "description": "Define structured output schema that Instructor validates from LLM responses",
        "code": "from instructor import from_openai\nfrom pydantic import BaseModel, Field\nfrom typing import List\nimport openai\n\nclass TestCase(BaseModel):\n    input: str\n    expected_output: str\n\nclass CodeSuggestion(BaseModel):\n    file_path: str = Field(..., description=\"Target file path\")\n    code_block: str = Field(..., description=\"Complete code replacement\")\n    test_cases: List[TestCase] = Field(default=[], description=\"Validation tests\")\n    explanation: str = Field(..., description=\"Why this fix works\")\n\nclient = from_openai(openai.OpenAI())",
        "language": "python"
      },
      {
        "title": "Basic Instructor Extraction",
        "description": "Wrap LLM call to extract validated code suggestions from repository context",
        "code": "repo_context = \"def buggy_function(x): return x * 'a'  # Bug: string multiplication\"\n\nsuggestion: CodeSuggestion = client.chat.completions.create(\n    model=\"gpt-4o\",\n    response_model=CodeSuggestion,\n    messages=[\n        {\"role\": \"system\", \"content\": \"You are a code review assistant. Always provide complete working fixes.\"},\n        {\"role\": \"user\", \"content\": f\"Fix this code:\\n{repo_context}\"}\n    ]\n)\nprint(suggestion.code_block)  # Guaranteed valid structure",
        "language": "python"
      },
      {
        "title": "TypeScript Instructor Equivalent",
        "description": "Zod schema for TypeScript code extraction with valibot or zod-instructor",
        "code": "import { OpenAI } from 'openai';\nimport { z } from 'zod';\nimport { createInstructor } from '@instructor-ai/instructor';\n\nconst CodeSuggestionSchema = z.object({\n  filePath: z.string().describe('Target file path'),\n  codeBlock: z.string().describe('Complete code replacement'),\n  testCases: z.array(z.object({\n    input: z.string(),\n    expectedOutput: z.string()\n  })),\n  explanation: z.string().describe('Why this fix works')\n});\n\ntype CodeSuggestion = z.infer<typeof CodeSuggestionSchema>;\n\nconst client = createInstructor({\n  client: new OpenAI(),\n  mode: 'json-schema'\n});",
        "language": "typescript"
      },
      {
        "title": "Sandbox Test Execution",
        "description": "Execute and validate Instructor-extracted code in isolated environment",
        "code": "import subprocess\nimport tempfile\nimport os\n\ndef validate_suggestion(suggestion: CodeSuggestion) -> bool:\n    with tempfile.TemporaryDirectory() as tmpdir:\n        filepath = os.path.join(tmpdir, suggestion.file_path)\n        os.makedirs(os.path.dirname(filepath), exist_ok=True)\n        \n        with open(filepath, 'w') as f:\n            f.write(suggestion.code_block)\n        \n        for test in suggestion.test_cases:\n            result = subprocess.run(\n                ['python', '-c', f'exec(open(\"{filepath}\").read()); print(buggy_function(\"{test.input}\"))'],\n                capture_output=True, text=True, cwd=tmpdir\n            )\n            if result.stdout.strip() != test.expected_output:\n                return False\n    return True",
        "language": "python"
      }
    ],
    "vexIntegration": [
      {
        "title": "Vex-Wrapped Instructor Client",
        "description": "Patch Instructor with Vex verification for code safety and schema compliance",
        "code": "from vex import verify\nfrom instructor import from_openai\nimport openai\n\n@verify(checks=[\n    'code-correctness',\n    'security-vulnerability-scanning',\n    'no-credential-leakage',\n    'dependency-safety',\n    'license-compliance'\n])\ndef safe_code_suggestion(repo_context: str) -> CodeSuggestion:\n    client = from_openai(openai.OpenAI())\n    return client.chat.completions.create(\n        model=\"gpt-4o\",\n        response_model=CodeSuggestion,\n        messages=[\n            {\"role\": \"system\", \"content\": \"Production-grade secure code only.\"},\n            {\"role\": \"user\", \"content\": repo_context}\n        ]\n    )\n\n# Usage\nsuggestion = safe_code_suggestion(buggy_code_context)\n# Vex blocks: eval(), __import__('os'), API keys, malicious deps",
        "language": "python"
      },
      {
        "title": "TypeScript Vex + Instructor",
        "description": "Verify structured outputs with Vex in Node.js/TypeScript agents",
        "code": "import { verify } from 'vex-sdk';\nimport { createInstructor } from '@instructor-ai/instructor';\nimport OpenAI from 'openai';\n\nconst openai = new OpenAI();\n\nconst safeCodeSuggestion = verify(\n  async (repoContext: string) => {\n    const client = createInstructor({ client: openai });\n    return client.chat.completions.create({\n      model: 'gpt-4o',\n      schema: CodeSuggestionSchema,\n      messages: [{ role: 'user', content: repoContext }]\n    });\n  },\n  {\n    checks: [\n      'code-correctness',\n      'security-vulnerability-scanning',\n      'no-credential-leakage',\n      'dependency-safety',\n      'license-compliance'\n    ]\n  }\n);\n\nconst suggestion = await safeCodeSuggestion(buggyCode);",
        "language": "typescript"
      },
      {
        "title": "Iterative Debug Loop with Vex",
        "description": "Handle Instructor validation failures with Vex observability and auto-retry",
        "code": "from vex import verify, on_failure\n\n@verify(\n    checks=['code-correctness', 'security-vulnerability-scanning'],\n    max_retries=3,\n    on_failure=on_failure('log-and-retry')\n)\ndef debug_loop(initial_error: str) -> CodeSuggestion:\n    client = from_openai(openai.OpenAI())\n    return client.chat.completions.create(\n        response_model=CodeSuggestion,\n        messages=[\n            {\"role\": \"system\", \"content\": \"Fix iteratively. Previous attempts failed validation.\"},\n            {\"role\": \"user\", \"content\": f\"Error: {initial_error}. Provide working fix:\"}\n        ]\n    )\n\n# Handles schema failures + security blocks automatically\nfixed_code = debug_loop('TypeError: string multiplication')",
        "language": "python"
      }
    ],
    "cta": {
      "heading": "Secure Your Instructor AI Coding Assistant",
      "description": "Install Vex today to protect your code generation pipeline from security risks and validation failures."
    }
  }
}
