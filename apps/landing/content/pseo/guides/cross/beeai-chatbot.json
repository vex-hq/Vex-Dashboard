{
  "meta": {
    "framework": "beeai",
    "useCase": "chatbot",
    "generatedAt": "2026-03-12"
  },
  "seo": {
    "title": "Building AI Chatbot with BeeAI + Vex Guardrails",
    "description": "Learn how to build ai chatbot with BeeAI + Vex guardrails. Detect failures, prevent drift, and ensure reliability.",
    "keywords": [
      "BeeAI ai chatbot",
      "BeeAI guardrails",
      "ai chatbot reliability",
      "Vex BeeAI",
      "AI agent guardrails"
    ]
  },
  "content": {
    "intro": "Building AI Chatbots with BeeAI enables robust conversational agents using IBM's open-source framework, featuring memory, tools, and observability for production-grade deployments. Guardrails are essential to address failure modes like unbounded memory growth, tool injection vulnerabilities, state serialization errors, and model output inconsistencies. Vex provides runtime verification to ensure response accuracy, coherence, safety, PII protection, and hallucination detection in high-risk chatbot scenarios.",
    "architectureOverview": {
      "heading": "AI Chatbot Architecture with BeeAI",
      "description": "The BeeAI AI Chatbot follows a retrieval-augmented generation (RAG) pattern with intent classification and multi-turn memory. User input triggers intent classification via a Granite model, followed by vector database retrieval for relevant context. The agent maintains conversation memory across turns, invokes tools for external actions, and generates responses with safety filtering.\n\nKey components include BeeAI's Agent class for orchestration, MemoryManager for session state, ToolRegistry for function calling, and Granite LLM integration for reasoning. Observability hooks capture events for monitoring and verification. This architecture handles complex multi-turn conversations while requiring strict guardrails for production reliability.\n\nVex integrates as an event listener to verify agent outputs against accuracy, coherence, safety, PII, and hallucination criteria before user delivery.",
      "diagram": "┌─────────────────┐    ┌──────────────────┐\n│     User Input  │───▶│ Intent Classifier │\n└─────────────────┘    │   (Granite LLM)   │\n                       └──────────────────┘\n                               │\n                       ┌──────────────────┐\n                       │ Vector DB RAG    │◄──┐\n                       │ Retrieval        │   │\n                       └──────────────────┘   │\n                               │              │\n                       ┌──────────────────┐   │\n                       │   BeeAI Agent    │───┘\n                       │ + MemoryManager  │\n                       │ + Tool Registry  │\n                       └──────────────────┘\n                               │\n                       ┌──────────────────┐\n                       │   Vex Guardrails │\n                       │ (Verification)   │\n                       └──────────────────┘\n                               │\n                       ┌──────────────────┐\n                       │  Final Response  │──▶ User\n                       └──────────────────┘"
    },
    "risks": [
      {
        "name": "Unbounded Memory Growth",
        "description": "Agent memory accumulates indefinitely in long conversations, causing OOM errors and degraded performance",
        "severity": "critical",
        "mitigation": "Vex monitors memory size per session and enforces truncation thresholds"
      },
      {
        "name": "Tool Injection Vulnerabilities",
        "description": "Malicious inputs bypass tool validation, enabling unauthorized function execution",
        "severity": "critical",
        "mitigation": "Vex validates tool parameters and sanitizes inputs before execution"
      },
      {
        "name": "PII Leakage in Responses",
        "description": "Chatbot exposes sensitive user data in generated responses or tool calls",
        "severity": "high",
        "mitigation": "Vex scans outputs for PII patterns and applies redaction/blocking"
      },
      {
        "name": "Hallucination in RAG Context",
        "description": "Agent generates factually incorrect responses despite retrieved context",
        "severity": "high",
        "mitigation": "Vex verifies response fidelity to retrieved documents and sources"
      }
    ],
    "implementation": [
      {
        "title": "Basic BeeAI Chatbot Setup",
        "description": "Initialize BeeAI agent with memory, tools, and Granite LLM for conversational RAG",
        "code": "from beeai import Agent, MemoryManager, ToolRegistry\nfrom beeai.llms import GraniteLLM\n\n# Initialize components\nllm = GraniteLLM(model='granite-3b-chat')\nmemory = MemoryManager(max_tokens=8000)\ntools = ToolRegistry()\n\nagent = Agent(\n    llm=llm,\n    memory=memory,\n    tools=tools,\n    system_prompt=\"You are a helpful chatbot assistant.\"\n)\n\n# Register RAG tool\n@tools.register\ndef search_knowledge_base(query: str) -> str:\n    # Vector DB retrieval logic\n    return 'Retrieved context about ' + query",
        "language": "python"
      },
      {
        "title": "Intent Classification Pipeline",
        "description": "Classify user intent and route to appropriate agent behavior",
        "code": "from beeai.events import EventListener\n\nclass IntentClassifier(EventListener):\n    def on_user_message(self, message: str) -> str:\n        intents = ['greeting', 'query', 'task', 'goodbye']\n        # Granite classification\n        intent = llm.classify(message, intents)\n        \n        if intent == 'query':\n            return agent.run(message)  # RAG path\n        elif intent == 'task':\n            return agent.run(message, tools=True)\n        return agent.run(message)\n\nclassifier = IntentClassifier()",
        "language": "python"
      },
      {
        "title": "Multi-turn Memory Management",
        "description": "Persist conversation state with automatic summarization for long sessions",
        "code": "def process_conversation(user_input: str) -> str:\n    # Add to memory\n    memory.add_user_message(user_input)\n    \n    # Check memory size\n    if memory.token_count > 6000:\n        summary = llm.summarize(memory.get_history())\n        memory.reset(summary)\n    \n    # Generate response\n    response = agent.run(user_input)\n    memory.add_assistant_message(response)\n    \n    return response\n\n# Usage\nresponse = process_conversation('What is RAG?')",
        "language": "python"
      },
      {
        "title": "Safety Filtering Wrapper",
        "description": "Basic content filtering before response delivery",
        "code": "import re\n\ndef safe_generate(user_input: str) -> str:\n    # Block harmful content\n    if re.search(r'\\b(?:kill|bomb|hack)\\b', user_input, re.IGNORECASE):\n        return \"I can't help with that request.\"\n    \n    response = agent.run(user_input)\n    \n    # Block PII patterns\n    pii_patterns = [r'\\b\\d{3}-\\d{2}-\\d{4}\\b', r'\\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Z|a-z]{2,}\\b']\n    for pattern in pii_patterns:\n        response = re.sub(pattern, '[REDACTED]', response)\n    \n    return response",
        "language": "python"
      }
    ],
    "vexIntegration": [
      {
        "title": "Register Vex Event Listener",
        "description": "Hook Vex into BeeAI's event system for comprehensive verification",
        "code": "from vex_sdk import VexClient\nfrom beeai.events import EventBus\n\n# Initialize Vex client\nvex = VexClient(api_key='your-vex-api-key')\n\nclass VexListener(EventListener):\n    def __init__(self):\n        self.verifications = [\n            'response_accuracy',\n            'conversation_coherence',\n            'safety_filtering',\n            'pii_protection',\n            'hallucination_detection'\n        ]\n    \n    def on_agent_response(self, response_data):\n        verdict = vex.verify(\n            agent_output=response_data['response'],\n            conversation_history=response_data['history'],\n            verifications=self.verifications,\n            risk_profile='high'\n        )\n        \n        if not verdict.approved:\n            return \"[VEX BLOCKED: \" + verdict.reason + \"]\"\n        return response_data['response']\n\n# Register listener\nvex_listener = VexListener()\nEventBus.register('agent_response', vex_listener.on_agent_response)",
        "language": "python"
      },
      {
        "title": "Memory Growth Monitoring",
        "description": "Prevent unbounded memory expansion with Vex limits",
        "code": "class VexMemoryGuard(EventListener):\n    def on_memory_update(self, memory_state):\n        metrics = vex.track_memory_growth(\n            session_id=memory_state.session_id,\n            token_count=len(memory_state.tokens),\n            max_allowed=8000\n        )\n        \n        if metrics.usage_ratio > 0.9:\n            # Auto-summarize\n            summary = llm.summarize(memory_state.history[-10:])\n            memory_state.truncate(summary)\n            vex.log_event('memory_truncated', {'tokens_saved': metrics.tokens_saved})\n        \n        return memory_state\n\nmemory_guard = VexMemoryGuard()\nEventBus.register('memory_update', memory_guard.on_memory_update)",
        "language": "python"
      },
      {
        "title": "Tool Call Verification",
        "description": "Validate tool parameters and prevent injection attacks",
        "code": "class VexToolGuard(EventListener):\n    def on_tool_call(self, tool_call):\n        # Verify tool parameters\n        validation = vex.validate_tool_call(\n            tool_name=tool_call.name,\n            parameters=tool_call.args,\n            allowed_tools=['search_knowledge_base'],\n            session_context=agent.memory.get_context()\n        )\n        \n        if not validation.safe:\n            vex.log_security_event('tool_injection_attempt', tool_call)\n            return {'error': 'Invalid tool usage detected'}\n        \n        # Execute validated tool\n        result = tools.execute(tool_call.name, **tool_call.args)\n        return {'result': result}\n\ntool_guard = VexToolGuard()\nEventBus.register('before_tool_call', tool_guard.on_tool_call)",
        "language": "python"
      }
    ],
    "cta": {
      "heading": "Secure Your BeeAI AI Chatbot",
      "description": "Get started with Vex free tier at vex.ai to protect your production chatbot today."
    }
  }
}
