{
  "meta": {
    "framework": "dspy",
    "useCase": "email-assistant",
    "generatedAt": "2026-03-12"
  },
  "seo": {
    "title": "Building AI Email Assistant with DSPy + Vex Guardrails",
    "description": "Learn how to build ai email assistant with DSPy + Vex guardrails. Detect failures, prevent drift, and ensure reliability.",
    "keywords": [
      "DSPy ai email assistant",
      "DSPy guardrails",
      "ai email assistant reliability",
      "Vex DSPy",
      "AI agent guardrails"
    ]
  },
  "content": {
    "intro": "Building an AI Email Assistant with DSPy enables programmatic LLM optimization for tasks like intent detection and draft generation, turning prompting into a structured programming paradigm. However, DSPy's optimized modules can overfit to training data or produce chain-of-thought errors that cascade through email workflows. Vex guardrails are essential to verify PII filtering, tone appropriateness, and confidentiality before emails reach real inboxes.",
    "architectureOverview": {
      "heading": "AI Email Assistant Architecture with DSPy",
      "description": "The AI Email Assistant processes incoming emails through a DSPy pipeline starting with an IntentClassifier module that detects user goals (reply, forward, draft). This feeds into specialized modules: DraftGenerator for content creation, AttachmentHandler for file analysis, and ToneAdjuster for sentiment optimization. A final ApprovalChain module orchestrates send decisions with human-in-loop validation.\n\nDSPy's optimizer tunes these modules against synthetic email datasets, but without runtime verification, optimized prompts can generate inappropriate tone or leak PII. The pipeline uses dspy.ChainOfThought and custom signatures for structured reasoning.\n\nVex wraps critical forward() calls to enforce verification at inference time, catching failures before email transmission.",
      "diagram": "┌─────────────────┐    ┌──────────────────┐    ┌──────────────────┐\n│   Incoming      │───▶│  IntentClassifier │───▶│   DraftGenerator  │\n│     Email       │    │   (dspy.module)   │    │   (dspy.module)   │\n└─────────────────┘    │     verify()      │    │     verify()      │\n                       └──────────────────┘    └──────────────────┘\n                                 │                       │\n                       ┌──────────────────┐    ┌──────────────────┐\n                       │ AttachmentHandler│◀──▶│   ToneAdjuster   │\n                       │   (dspy.module)  │    │   (dspy.module)  │\n                       │     verify()     │    │     verify()     │\n                       └──────────────────┘    └──────────────────┘\n                                 │\n                       ┌──────────────────┐\n                       │  ApprovalChain   │───▶┌──────────────┐\n                       │   (dspy.module)  │    │   Send       │\n                       │     verify()     │    │   Email      │\n                       └──────────────────┘    └──────────────┘"
    },
    "risks": [
      {
        "name": "PII Leakage in Drafts",
        "description": "Optimized DraftGenerator modules extract and include sensitive data (SSNs, emails) from context",
        "severity": "critical",
        "mitigation": "Vex PII scanner on all generated email content before approval"
      },
      {
        "name": "Inappropriate Tone",
        "description": "ToneAdjuster produces sarcastic/hostile language due to CoT reasoning errors",
        "severity": "high",
        "mitigation": "Vex sentiment analysis with professional tone assertions"
      },
      {
        "name": "Invalid Recipients",
        "description": "IntentClassifier misdetects forwarding targets, sending to wrong domains",
        "severity": "critical",
        "mitigation": "Vex recipient domain validation against approved lists"
      },
      {
        "name": "Confidentiality Breaches",
        "description": "AttachmentHandler references internal docs in external replies",
        "severity": "high",
        "mitigation": "Vex confidentiality classifier blocking internal references"
      }
    ],
    "implementation": [
      {
        "title": "Intent Detection Module",
        "description": "DSPy signature for classifying email intent with few-shot optimization",
        "code": "import dspy\n\nclass IntentClassifier(dspy.Signature):\n    '''Classify email processing intent.'''\n    email_content = dspy.InputField()\n    intent = dspy.OutputField(desc='reply|draft|forward|archive')\n\nclass IntentModule(dspy.Module):\n    def __init__(self):\n        super().__init__()\n        self.classify = dspy.ChainOfThought(IntentClassifier)\n    \n    def forward(self, email_content: str) -> dspy.Prediction:\n        return self.classify(email_content=email_content)\n\n# Usage\nlm = dspy.OpenAI(model='gpt-4')\ndspy.settings.configure(lm=lm)\nmodule = IntentModule()\nresult = module('Urgent: Review Q4 financials')",
        "language": "python"
      },
      {
        "title": "Draft Generation Module",
        "description": "Generates context-aware email replies with tone control",
        "code": "class DraftGenerator(dspy.Signature):\n    '''Generate professional email draft.'''\n    intent: str = dspy.InputField()\n    context: str = dspy.InputField()\n    draft: str = dspy.OutputField(desc='Professional email draft, max 200 words')\n\nclass DraftModule(dspy.Module):\n    def __init__(self):\n        super().__init__()\n        self.generate = dspy.ChainOfThought(DraftGenerator)\n    \n    def forward(self, intent: str, context: str) -> dspy.Prediction:\n        return self.generate(intent=intent, context=context)\n\n# Optimized usage\nmodule = DraftModule()\nresult = module('reply', 'Customer reported billing issue on invoice #12345')",
        "language": "python"
      },
      {
        "title": "Attachment Handler",
        "description": "Analyzes email attachments for relevance and sensitivity",
        "code": "class AttachmentAnalyzer(dspy.Signature):\n    '''Analyze email attachment content.'''\n    filename: str = dspy.InputField()\n    content_summary: str = dspy.InputField()\n    action = dspy.OutputField(desc='include|exclude|redact|flag')\n    reason: str = dspy.OutputField(desc='Brief explanation')\n\nclass AttachmentModule(dspy.Module):\n    def __init__(self):\n        super().__init__()\n        self.analyze = dspy.ChainOfThought(AttachmentAnalyzer)\n    \n    def forward(self, filename: str, content_summary: str) -> dspy.Prediction:\n        return self.analyze(filename=filename, content_summary=content_summary)\n\nmodule = AttachmentModule()\nresult = module('financials.xlsx', 'Contains Q4 revenue data and employee salaries')",
        "language": "python"
      },
      {
        "title": "Send Approval Chain",
        "description": "Final validation before email transmission",
        "code": "class ApprovalChain(dspy.Module):\n    def __init__(self):\n        super().__init__()\n        self.intent = IntentModule()\n        self.draft = DraftModule()\n        self.attach = AttachmentModule()\n    \n    def forward(self, email_content: str, recipient: str) -> dict:\n        intent_pred = self.intent(email_content)\n        draft_pred = self.draft(intent_pred.intent, email_content)\n        attach_pred = self.attach('invoice.pdf', 'Billing details')\n        \n        return {\n            'intent': intent_pred.intent,\n            'draft': draft_pred.draft,\n            'attachments': [attach_pred.action],\n            'approved': 'pending'\n        }",
        "language": "python"
      }
    ],
    "vexIntegration": [
      {
        "title": "PII Filtering Wrapper",
        "description": "Wraps draft generation with PII detection using Vex verify()",
        "code": "import vex\n\n@vex.verify(\n    assertions=[\n        {'type': 'pii_filter', 'PII_ENTITIES': ['SSN', 'EMAIL', 'PHONE'], 'action': 'block'},\n        {'type': 'regex', 'pattern': r'\\b\\d{3}-\\d{2}-\\d{4}\\b', 'action': 'block'}\n    ]\n)\ndef safe_draft_module(email_content: str) -> str:\n    module = DraftModule()\n    result = module('reply', email_content)\n    return result.draft\n\n# Usage - blocks PII leaks\ntry:\n    draft = safe_draft_module('Contact John Doe SSN 123-45-6789')\nexcept vex.ViolationError:\n    print('Draft blocked: PII detected')",
        "language": "python"
      },
      {
        "title": "Tone Appropriateness Check",
        "description": "Verifies professional tone before sending",
        "code": "@vex.verify(\n    assertions=[\n        {\n            'type': 'sentiment',\n            'min_score': 0.7,\n            'categories': ['professional', 'neutral'],\n            'action': 'block'\n        },\n        {'type': 'toxicity', 'max_score': 0.1, 'action': 'block'}\n    ]\n)\ndef safe_tone_adjuster(draft: str) -> str:\n    module = ToneModule()\n    result = module(draft)\n    return result.adjusted_draft\n\n# Blocks inappropriate language\nsafe_tone_adjuster('This is completely unacceptable! Fix it now!')",
        "language": "python"
      },
      {
        "title": "Recipient + Confidentiality Guard",
        "description": "Validates recipients and blocks confidential leaks",
        "code": "@vex.verify(\n    assertions=[\n        {\n            'type': 'recipient_validation',\n            'allowed_domains': ['@company.com', '@partner.com'],\n            'action': 'block'\n        },\n        {\n            'type': 'keyword_classifier',\n            'blocklist': ['confidential', 'internal-only', 'proprietary'],\n            'context_radius': 50,\n            'action': 'block'\n        }\n    ]\n)\ndef safe_approval_chain(email_data: dict) -> dict:\n    chain = ApprovalChain()\n    return chain(**email_data)\n\n# Production usage\nresult = safe_approval_chain({\n    'email_content': 'Q4 confidential financials...',\n    'recipient': 'competitor@evilcorp.com'\n})",
        "language": "python"
      }
    ],
    "cta": {
      "heading": "Secure Your DSPy AI Email Assistant",
      "description": "Add Vex verification to your DSPy modules today to prevent PII leaks and compliance violations."
    }
  }
}
