{
  "meta": {
    "framework": "vercel-ai-sdk",
    "useCase": "chatbot",
    "generatedAt": "2026-03-12"
  },
  "seo": {
    "title": "Building AI Chatbot with Vercel AI SDK + Vex Guardrails",
    "description": "Learn how to build ai chatbot with Vercel AI SDK + Vex guardrails. Detect failures, prevent drift, and ensure reliability.",
    "keywords": [
      "Vercel AI SDK ai chatbot",
      "Vercel AI SDK guardrails",
      "ai chatbot reliability",
      "Vex Vercel AI SDK",
      "AI agent guardrails"
    ]
  },
  "content": {
    "intro": "Building AI chatbots with Vercel AI SDK enables seamless streaming UIs and conversational agents using React Server Components. However, complex multi-turn conversations with RAG, intent classification, and tool calls introduce risks like hallucinations, PII leaks, and incoherent responses. Vex provides essential guardrails to verify response accuracy, safety, and coherence before reaching users.",
    "architectureOverview": {
      "heading": "AI Chatbot Architecture with Vercel AI SDK",
      "description": "The typical Vercel AI SDK chatbot architecture uses API routes or Server Actions to handle streaming conversations. User messages trigger intent classification, RAG retrieval from vector stores, and LLM calls via generateText or streamText. Multi-turn memory is managed through session state or databases, with tool calls for external actions like database queries or API integrations.\n\nEdge functions process requests with providers like OpenAI or Anthropic, streaming tokens back to React components via useChat hook. Common patterns include structured output extraction for intent/tools and partial response accumulation for real-time display. Failures occur when unvalidated tool results display directly or streaming bypasses safety checks.\n\nVex integrates as middleware to intercept generateText/streamText calls, verifying outputs against accuracy, safety, and coherence before forwarding to the UI.",
      "diagram": "┌─────────────────┐    ┌──────────────────┐\n│   React UI      │◄──►│ Vercel API Route │\n│ (useChat hook)  │    │   (streamText)   │\n└─────────────────┼────┼──────────────────┤\n                 │    │    ┌─────────────┐ │\n                 ▼    ▼    │   Vex       │ ▼\n┌─────────────────┐ ┌──────┴──┐        │ LLM\n│ Session Memory  │ │ RAG     │        │ Provider\n│   (Redis/DB)    │ │ Vector  │◄───────┘\n└─────────────────┘ │ Store   │\n                    └────────┘"
    },
    "risks": [
      {
        "name": "Streaming responses with partial hallucinations",
        "description": "Partial token streams display unverified content containing factual errors before full response completion",
        "severity": "critical",
        "mitigation": "Vex stream verification buffers and validates partial responses before UI rendering"
      },
      {
        "name": "Tool call results not validated before display",
        "description": "Unverified tool outputs containing malicious payloads or incorrect data displayed directly to users",
        "severity": "critical",
        "mitigation": "Vex tool result validation ensures accuracy and safety before response incorporation"
      },
      {
        "name": "Inconsistent structured output across providers",
        "description": "Intent classification and tool schemas fail across different LLM providers due to varying structured output reliability",
        "severity": "high",
        "mitigation": "Vex normalizes and verifies structured outputs regardless of provider implementation"
      },
      {
        "name": "Edge function timeouts on complex agent tasks",
        "description": "Multi-step reasoning chains exceed Vercel edge function timeouts, causing incomplete conversations",
        "severity": "high",
        "mitigation": "Vex task decomposition and parallel verification prevents timeout cascades"
      }
    ],
    "implementation": [
      {
        "title": "Basic Streaming Chatbot Setup",
        "description": "Core Vercel AI SDK streaming implementation with useChat integration",
        "code": "import { createStreamableUI } from 'ai/rsc';\nimport { streamText } from 'ai';\nimport { openai } from '@ai-sdk/openai';\n\nexport async function ChatAPI() {\n  const { streamText: stream, sendMessage } = createStreamableUI();\n\n  return {\n    streamText: async ({ messages }) => {\n      const result = await streamText({\n        model: openai('gpt-4o'),\n        messages,\n      });\n      return stream(result.textStream);\n    },\n    sendMessage,\n  };\n}",
        "language": "typescript"
      },
      {
        "title": "Intent Classification with Structured Output",
        "description": "Extract conversation intent using consistent structured output across providers",
        "code": "import { generateText } from 'ai';\nimport { z } from 'zod';\n\nconst intentSchema = z.object({\n  intent: z.enum(['greeting', 'query', 'task', 'goodbye']),\n  confidence: z.number().min(0).max(1),\n});\n\nexport async function classifyIntent(messages: any[]) {\n  const { text } = await generateText({\n    model: openai('gpt-4o'),\n    schema: intentSchema,\n    prompt: `Classify user intent from conversation: ${{messages}}`,  \n  });\n  return text;\n}",
        "language": "typescript"
      },
      {
        "title": "RAG Integration with Vector Retrieval",
        "description": "Retrieval-augmented generation using external vector store",
        "code": "import { streamText } from 'ai';\nimport { openai } from '@ai-sdk/openai';\n\nexport async function ragStream(messages: any[]) {\n  // Mock vector retrieval\n  const context = await retrieveRelevantDocs(messages[messages.length-1].content);\n  \n  const result = await streamText({\n    model: openai('gpt-4o'),\n    messages: [\n      { role: 'system', content: `Use this context: ${context}` },\n      ...messages,\n    ],\n  });\n  \n  return result;\n}\n\nasync function retrieveRelevantDocs(query: string) {\n  // Pinecone/Supabase vector search\n  return 'Retrieved documents...';\n}",
        "language": "typescript"
      },
      {
        "title": "Multi-turn Memory Management",
        "description": "Session-based conversation history persistence",
        "code": "import { cookies } from 'next/headers';\n\nexport function getConversationHistory(sessionId: string) {\n  const cookieStore = cookies();\n  const history = cookieStore.get(`chat:${sessionId}`)?.value;\n  return history ? JSON.parse(history) : [];\n}\n\nexport function saveConversation(sessionId: string, messages: any[]) {\n  cookies().set(`chat:${sessionId}`, JSON.stringify(messages), {\n    maxAge: 60 * 60 * 24 * 7, // 7 days\n  });\n}",
        "language": "typescript"
      }
    ],
    "vexIntegration": [
      {
        "title": "Vex Middleware for generateText",
        "description": "Wrap generateText calls with Vex verification for accuracy and safety",
        "code": "import { createVex } from '@vex_dev/sdk';\nimport { generateText } from 'ai';\n\nconst vex = createVex({\n  projectId: process.env.VEX_PROJECT_ID!,\n  apiKey: process.env.VEX_API_KEY!,\n});\n\nexport async function verifiedGenerateText(params: any) {\n  const result = await generateText(params);\n  \n  const verified = await vex.verify({\n    input: params.messages,\n    output: result.text,\n    checks: ['accuracy', 'safety', 'pii'],\n    context: { useCase: 'chatbot' },\n  });\n  \n  if (!verified.passes) {\n    throw new Error(`Verification failed: ${verified.issues.join(', ')}`);\n  }\n  \n  return result;\n}",
        "language": "typescript"
      },
      {
        "title": "Vex Stream Verification Middleware",
        "description": "Buffer and verify streaming responses to catch partial hallucinations",
        "code": "import { createVex } from '@vex_dev/sdk';\nimport { streamText } from 'ai';\n\nconst vex = createVex({ /* config */ });\n\nexport async function verifiedStreamText(params: any) {\n  let fullResponse = '';\n  const chunks: string[] = [];\n  \n  const result = await streamText(params);\n  \n  const stream = new ReadableStream({\n    async start(controller) {\n      for await (const chunk of result.textStream) {\n        fullResponse += chunk;\n        chunks.push(chunk);\n        \n        // Verify every 10 chunks or 500 chars\n        if (chunks.length % 10 === 0 || fullResponse.length > 500) {\n          const verified = await vex.verify({\n            input: params.messages,\n            output: fullResponse,\n            checks: ['hallucination', 'coherence'],\n          });\n          \n          if (!verified.passes) {\n            controller.error(new Error('Stream verification failed'));\n            return;\n          }\n        }\n        \n        controller.enqueue(chunk);\n      }\n      controller.close();\n    },\n  });\n  \n  return { textStream: stream, fullText: () => fullResponse };\n}",
        "language": "typescript"
      },
      {
        "title": "Tool Call Result Validation",
        "description": "Verify tool outputs before incorporating into conversation flow",
        "code": "import { createVex } from '@vex_dev/sdk';\n\nconst vex = createVex({ /* config */ });\n\nexport async function verifyToolResult(toolCall: any, result: any) {\n  const verified = await vex.verify({\n    input: toolCall.args,\n    output: JSON.stringify(result),\n    checks: ['accuracy', 'safety', 'pii'],\n    context: {\n      toolName: toolCall.name,\n      useCase: 'agent-tool-call',\n    },\n  });\n  \n  if (!verified.passes) {\n    return {\n      role: 'assistant',\n      content: 'I encountered an issue processing that request. Could you try rephrasing?',\n    };\n  }\n  \n  return {\n    role: 'tool',\n    tool_call_id: toolCall.id,\n    content: JSON.stringify(result),\n  };\n}",
        "language": "typescript"
      }
    ],
    "cta": {
      "heading": "Secure Your Vercel AI SDK AI Chatbot",
      "description": "Add Vex guardrails today to protect your users from hallucinations, PII leaks, and incoherent responses."
    }
  }
}
